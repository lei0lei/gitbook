# Game Loop

### 意图

将游戏时间的推进与用户输入和处理器速度解耦。

### 动机

如果说本书有哪个模式绝对不能缺少，那就是游戏循环。游戏循环是“游戏编程模式”最典型的例子：几乎每款游戏都有一个，没有两个游戏循环完全相同，而游戏之外的程序很少使用它。

为了理解它的作用，让我们沿着记忆长河快速回到过去。

在计算机编程的远古年代——那时每个人都留着胡子——程序的工作方式和洗碗机差不多：塞进去一批代码，按下按钮，等待一段时间，然后得到结果。任务结束，程序也随之停止。

这类程序称为批处理程序。

Ada Lovelace 和海军少将 Grace Hopper 也拥有荣誉胡须。

如今仍然能看到批处理程序，不过值得庆幸的是，我们已经无须在穿孔卡片上编写它们了。Shell 脚本、命令行程序，甚至把一堆 Markdown 转换成本书的那个小型 Python 脚本，都属于批处理程序。

### 与 CPU 的访谈

后来，程序员意识到，把一批代码送到计算中心，再过几个小时回来取结果，是一种效率极低的程序调试方式。他们希望立即得到反馈，于是交互式程序诞生了。

最早的一批交互式程序中就包括游戏：

```
// Some code
你站在一条道路的尽头，面前是一座小砖房。
周围是一片森林。一条小溪从建筑中流出，
沿着沟壑向下流去。

> 进去

你位于一座建筑内部。这是一间建在巨大泉眼上的井房。
```

这是《巨洞冒险》（Colossal Cave Adventure），世界上第一款冒险游戏。

你可以与程序进行实时对话。程序等待你的输入，然后作出回应。接着你再回复它，双方像幼儿园里学到的那样轮流交谈。轮到你时，程序就坐在那里什么也不做。

实现大致如下：

```
// Some code
while (true)
{
  char* command = readCommand();
  handleCommand(command);
}
```

这个循环会永远运行，因此无法退出游戏。真正的游戏可能会使用 `while (!done)`，并在退出时设置 `done`。为了保持简单，这里省略了相关逻辑。

### 事件循环

剥掉现代图形界面应用程序的外皮后，你会发现它们与老式冒险游戏惊人地相似。文字处理器通常也只是坐在那里什么都不做，直到你按下某个键或点击某个地方：

```
// Some code
while (true)
{
  Event* event = waitForEvent();
  dispatchEvent(event);
}
```

主要区别在于，程序等待的不再是文本命令，而是用户输入事件，例如鼠标点击和按键。

它的工作方式仍然和老式文字冒险游戏基本相同：程序会阻塞并等待用户输入。而这对游戏来说是个问题。

与大多数其他软件不同，即使玩家没有提供输入，游戏仍然会继续运行。你坐在那里盯着屏幕时，游戏并不会冻结：

* 动画仍在播放；
* 视觉效果仍在飞舞闪烁；
* 如果运气不好，怪物还会继续啃咬你的英雄。

大多数事件循环确实会提供“空闲”事件，让程序可以在没有用户输入时偶尔执行一些工作。这足以实现闪烁光标或进度条，却过于简陋，无法满足游戏的需要。

这就引出了真正游戏循环的第一个关键特征：

> 它会处理用户输入，但不会等待用户输入。

循环始终不断运行：

```
// Some code
while (true)
{
  processInput();
  update();
  render();
}
```

稍后还会改进这段代码，但基本组成部分已经齐全：

1. `processInput()` 处理自上次调用以来发生的所有用户输入。
2. `update()` 将游戏模拟向前推进一步。它会运行 AI 和物理系统——通常按照这个顺序。
3. `render()` 绘制游戏，让玩家看到刚刚发生的事情。

正如你可能根据名称猜到的，`update()` 很适合使用更新方法模式（Update Method）。

### 一个脱离时间的世界

既然这个循环不会为了等待输入而阻塞，一个显而易见的问题随之出现：它会运行得多快？

每执行一轮游戏循环，游戏状态都会向前推进一定程度。从游戏世界居民的角度来看，他们的时钟指针向前跳动了一下。

游戏循环每执行一轮，通常称为一个“滴答”（tick）或一“帧”（frame）。

与此同时，玩家现实世界中的时钟也在走动。如果使用现实时间衡量游戏循环的运行速度，就得到了游戏的“每秒帧数”（Frames Per Second，FPS）。

如果游戏循环运行得很快，FPS 就很高，游戏看起来流畅而迅速。如果循环运行缓慢，游戏就会像定格动画一样一顿一顿地前进。

对于目前这个只会尽可能快速运行的简陋循环，帧率由两个因素决定。

第一，每帧需要完成多少工作。复杂的物理运算、大量游戏对象和丰富的图形细节都会让 CPU 和 GPU 忙碌，使一帧需要更长时间才能完成。

第二，底层平台的运行速度。更快的芯片可以在相同时间内执行更多代码。多核 CPU、GPU、专用音频硬件以及操作系统的调度器，都会影响每个滴答中能够完成多少工作。

### 每秒里的游戏秒数

在早期电子游戏中，第二个因素是固定的。如果为 NES 或 Apple IIe 开发游戏，你会确切知道游戏运行在哪种 CPU 上，因此可以——而且当时的人们确实会——专门针对该处理器编写代码。

唯一需要担心的，就是每个滴答执行多少工作。

老游戏会经过精心编写，让每帧完成的工作量恰到好处，使游戏以开发者期望的速度运行。但如果尝试在更快或更慢的机器上运行同一款游戏，游戏本身也会相应加速或减速。

这就是老式 PC 曾经带有“Turbo”按钮的原因。新 PC 速度更快，导致老游戏运行得太快而无法正常游玩。关闭 Turbo 按钮会降低机器速度，使老游戏重新变得可以游玩。

但如今，很少有开发者能享受到预先知道游戏会运行在哪种硬件上的奢侈条件。我们的游戏必须智能地适应各种设备。

这就是游戏循环的另一个关键职责：

> 无论底层硬件存在怎样的差异，它都要让游戏以稳定一致的速度运行。

### 模式

游戏循环会在游戏过程中持续运行。循环的每一轮都会：

1. 在不阻塞的情况下处理用户输入；
2. 更新游戏状态；
3. 渲染游戏。

它还会追踪时间的流逝，以控制游戏的运行速度。

### 何时使用

使用错误的模式可能比完全不使用模式更加糟糕，因此这一节通常会提醒你不要过度热衷于某种模式。设计模式的目标不是让你在代码库中塞入尽可能多的模式。

不过，这个模式有点不同。我可以相当肯定地说，你一定会使用它。如果正在使用游戏引擎，可能不需要亲自编写游戏循环，但它仍然存在于引擎内部。

对我而言，这正是“引擎”和“库”的区别：

* 使用库时，你拥有主游戏循环，并从中调用库。
* 使用引擎时，引擎拥有循环，并从中调用你的代码。

如果制作的是回合制游戏，你可能认为不需要游戏循环。但即使在回合制游戏中，虽然游戏状态要等到玩家采取行动后才会推进，游戏的视觉和音频状态通常仍然会继续变化。

即使游戏正在“等待”你进行回合操作，动画和音乐也仍然会继续播放。

### 注意事项

这里讨论的循环，是游戏中最重要的代码之一。人们常说，程序会将 90% 的时间花在 10% 的代码上，而游戏循环肯定属于那 10%。

请谨慎对待这段代码，并时刻注意它的运行效率。

正是因为会编造这种统计数字，机械工程师和电气工程师之类的“真正”工程师才不把我们当回事。

#### 可能需要与平台的事件循环协调

如果游戏建立在拥有图形 UI 和内置事件循环的操作系统或平台之上，就会同时存在两个应用程序循环。它们需要和平共处。

有时，你可以接管控制权，只保留自己的游戏循环。例如，如果使用古老而可靠的 Windows API 编写游戏，可以直接在 `main()` 中运行游戏循环。

循环内部可以调用 `PeekMessage()`，处理并分发操作系统事件。与 `GetMessage()` 不同，`PeekMessage()` 不会阻塞并等待用户输入，因此游戏循环可以继续运行。

其他平台不会让你如此轻易地摆脱其事件循环。如果目标平台是网页浏览器，事件循环已经深深嵌入浏览器的执行模型。

这种情况下，浏览器的事件循环将掌控全局，你也会把它作为游戏循环使用。你会调用类似 `requestAnimationFrame()` 的函数，然后由浏览器回调你的代码，使游戏继续运行。

### 示例代码

虽然引言很长，但游戏循环本身的代码其实相当简单。接下来会介绍几种不同的实现，并分析各自的优缺点。

游戏循环会驱动 AI、渲染以及游戏中的其他系统，但这些系统并不是该模式本身的重点，所以这里只会调用一些虚构的方法。

至于如何真正实现 `render()`、`update()` 等函数，就留给读者作为一项富有挑战性的练习吧！

### 跑啊，能跑多快就跑多快

我们已经见过最简单的游戏循环：

```
// Some code
while (true)
{
  processInput();
  update();
  render();
}
```

它的问题在于，你完全无法控制游戏的运行速度。

在速度很快的机器上，这个循环会快到玩家根本看不清发生了什么。在缓慢的机器上，游戏则会像蜗牛一样爬行。如果游戏中的某个区域内容特别丰富，或者需要执行更多 AI 或物理运算，那么游戏在该区域甚至会实际变慢。

### 小睡一会儿

我们要看的第一种变体使用了一个简单的修复方案。

假设希望游戏以 60 FPS 运行，那么每帧大约拥有 16 毫秒。只要能够可靠地在这段时间内完成所有游戏处理和渲染，就可以维持稳定的帧率。

我们只需处理当前帧，然后等待下一帧应该开始的时间：

<figure><img src=".gitbook/assets/image (18).png" alt=""><figcaption></figcaption></figure>

代码大致如下：

> 1000 毫秒 ÷ FPS = 每帧的毫秒数。

```
// Some code
while (true)
{
  double start = getCurrentTime();

  processInput();
  update();
  render();

  sleep(start + MS_PER_FRAME - getCurrentTime());
}
```

这里的 `sleep()` 可以确保游戏在快速处理完一帧时不会运行得太快。但如果游戏运行得太慢，它就无能为力了。

如果更新和渲染一帧花费的时间超过 16 毫秒，休眠时间就会变成负数。如果计算机能够穿越时间，很多事情都会简单得多，可惜它们做不到。

因此，游戏只能减速。

你可以减少每帧的工作量来规避这个问题，例如降低图形效果和各种华丽特效，或者让 AI 变得更笨。但这会降低所有玩家的游戏体验，即使他们使用的是高性能机器。

### 一小步，一大步

让我们尝试一种更加复杂的方案。目前的问题基本可以归结为：

1. 每次更新都会让游戏时间向前推进一定长度。
2. 执行这次更新需要耗费一定长度的现实时间。

如果第二步花费的时间比第一步推进的游戏时间更长，游戏就会变慢。如果推进 16 毫秒的游戏时间需要超过 16 毫秒的处理时间，它就不可能跟上现实时间。

但是，如果一次更新能够让游戏时间推进超过 16 毫秒，即使降低更新频率，游戏仍然可以跟上现实时间。

因此，我们可以根据距离上一帧经过的现实时间，决定本次更新要推进多大的时间步长。一帧耗时越长，游戏就迈出越大的一步。游戏会不断增大步长来追赶现实时间，因此始终能够保持同步。

这种方式称为可变时间步长（variable time step），有时也称为流动时间步长（fluid time step）：

```
// Some code
double lastTime = getCurrentTime();

while (true)
{
  double current = getCurrentTime();
  double elapsed = current - lastTime;

  processInput();
  update(elapsed);
  render();

  lastTime = current;
}
```

每一帧，我们都会计算自上次更新游戏以来经过了多少现实时间，也就是 `elapsed`。更新游戏状态时，将这个值传入。随后，引擎负责让游戏世界向前推进相应的时间。

假设有一颗子弹正在飞过屏幕。

采用固定时间步长时，每一帧都会根据子弹的速度移动它。采用可变时间步长时，则要将速度乘以经过的时间。时间步长越大，子弹每帧移动的距离就越远。

无论子弹通过二十个短小而快速的步骤，还是四个漫长而缓慢的步骤飞过屏幕，它花费的现实时间都相同。

这个方案看起来很不错：

* 游戏在不同硬件上都能以一致的速度运行。
* 使用更快机器的玩家可以获得更加流畅的游戏体验。

然而，前方潜伏着一个严重问题：我们让游戏变得不确定而且不稳定了。

下面是我们为自己设下的陷阱之一。

“确定性”意味着每次运行程序时，只要提供相同的输入，就一定会得到完全相同的输出。可以想象，确定性程序中的缺陷更容易排查：只要找到第一次触发缺陷的输入，就能在之后的每一次运行中重现它。

计算机天然具有确定性，因为它们会机械地执行程序。当混乱的现实世界渗入程序时，非确定性才会出现。例如，网络、系统时钟和线程调度都依赖程序无法控制的外部环境。

假设我们正在制作一款双人联网游戏。Fred 拥有一台性能怪兽般的游戏电脑，而 George 使用的则是他祖母留下来的古董 PC。

前面提到的那颗子弹正同时飞过两人的屏幕。在 Fred 的机器上，游戏运行得极快，因此每个时间步长都很小。子弹飞过屏幕的一秒内，游戏大概能塞进 50 帧。可怜的 George 的机器则只能处理大约五帧。

这意味着 Fred 的物理引擎会更新子弹位置 50 次，而 George 的机器只会更新五次。

大多数游戏使用浮点数，而浮点运算会产生舍入误差。每次将两个浮点数相加，得到的结果都可能出现一点偏差。Fred 的机器执行了十倍的运算，因此积累的误差也会比 George 更大。同一颗子弹最终会在两台机器上落到不同的位置。

这只是可变时间步长可能造成的一个棘手问题，除此之外还有更多问题。

为了实时运行，游戏物理引擎只能对真实世界的力学定律进行近似模拟。为了防止这些近似计算失控，物理引擎会施加阻尼，而阻尼通常是针对特定时间步长精心调校的。一旦时间步长发生变化，物理模拟就可能变得不稳定。

这里的“失控爆炸”甚至可以按字面意思理解。当物理引擎发疯时，物体可能获得完全错误的速度，直接把自己发射到空中。

这种不稳定性非常严重。因此，这个示例只是一个警示故事，同时也用于引出更好的方案……

### 追赶时间

引擎中通常不会受到可变时间步长影响的部分是渲染。

渲染引擎捕捉的是某个时间点的瞬间状态，因此它并不关心距离上次渲染究竟推进了多少时间。它只会按照物体此刻所处的位置进行绘制。

更准确地说，大体如此。运动模糊等效果可能会受到时间步长影响，但即使存在少量偏差，玩家通常也不会注意到。

我们可以利用这一点。游戏更新采用固定时间步长，让物理和 AI 保持简单而稳定；渲染时机则保持灵活，以便释放一部分处理器时间。

其工作方式如下：

自游戏循环上一轮以来，已经过去了一定长度的现实时间。这就是我们需要模拟的游戏时间，以便让游戏中的“现在”追上玩家现实世界中的现在。

我们通过一系列固定时间步长来完成追赶。代码大致如下：

```
// Some code
double previous = getCurrentTime();
double lag = 0.0;

while (true)
{
  double current = getCurrentTime();
  double elapsed = current - previous;
  previous = current;
  lag += elapsed;

  processInput();

  while (lag >= MS_PER_UPDATE)
  {
    update();
    lag -= MS_PER_UPDATE;
  }

  render();
}
```

这里包含几个部分。

每帧开始时，我们根据已经过去的现实时间更新 `lag`。它衡量游戏时钟相对于现实世界落后了多少。

随后，进入内层循环，每次以固定时间步长更新游戏，直到游戏时间追上现实时间。完成追赶后，我们渲染游戏，再重新开始下一轮循环。

可以将这个过程大致想象成下图所示的形式：

<figure><img src=".gitbook/assets/image (19).png" alt=""><figcaption></figcaption></figure>

注意，这里的时间步长已经不再等同于玩家看到的帧率。`MS_PER_UPDATE` 只是更新游戏时采用的时间粒度。

时间步长越短，追赶现实时间所需的处理时间就越多；时间步长越长，游戏表现就越不连贯。理想情况下，应该使用相当短的时间步长——通常比 60 FPS 对应的间隔还短——从而让游戏在高性能机器上进行高精度模拟。

但要小心，不要把时间步长设置得太短。即使在最慢的目标硬件上，时间步长也必须大于执行一次 `update()` 所需的时间。否则，游戏根本不可能追上现实时间。

这里没有展示相关代码，但可以为内层更新循环设置最大迭代次数作为保护。超过这个次数后，循环会提前退出。此时游戏虽然会减速，但总比彻底卡死要好。

幸运的是，这种方案已经为我们争取了一些喘息空间。关键技巧在于，我们把渲染移出了更新循环，从而释放了大量 CPU 时间。

最终，游戏可以在不同性能的硬件上使用安全的固定时间步长，以恒定速率进行模拟。只不过在速度较慢的机器上，玩家用于观察游戏世界的可视窗口会变得不那么流畅。

### 卡在两次更新之间

现在还剩下一个问题：残余延迟。

我们以固定时间步长更新游戏，却会在任意时间点进行渲染。这意味着从玩家的角度来看，游戏经常需要显示两次更新之间某个时刻的状态。

下面是一条时间线：

<figure><img src=".gitbook/assets/image (20).png" alt=""><figcaption></figcaption></figure>

如你所见，我们以紧凑而固定的时间间隔更新游戏；与此同时，只要条件允许就进行渲染。

渲染频率比更新频率低，而且并不稳定。不过，这两点都没有问题。真正麻烦的是，渲染并不总是恰好发生在更新的时间点。

看看第三次渲染，它正好位于两次更新之间：

<figure><img src=".gitbook/assets/image (21).png" alt=""><figcaption></figcaption></figure>

假设一颗子弹正飞过屏幕。第一次更新时，它位于屏幕左侧；第二次更新将它移动到右侧。游戏在两次更新之间的某个时刻进行渲染，因此玩家期望看到子弹位于屏幕中央。

但按照当前实现，子弹仍然会显示在左侧。这会让运动看起来参差不齐、断断续续。

方便的是，渲染时我们其实确切知道自己位于两次更新之间的什么位置：这个信息保存在 `lag` 中。我们会在 `lag` 小于更新时间步长时退出更新循环，而不是等到它变成零。剩下的这部分时间，就是我们已经进入下一帧的程度。

渲染时，将这个值传进去：

```
// Some code
render(lag / MS_PER_UPDATE);
```

这里除以 `MS_PER_UPDATE`，是为了将数值归一化。无论更新时间步长是多少，传给 `render()` 的值都会介于 0——恰好位于上一帧——和略小于 1.0——即将到达下一帧——之间。

这样，渲染器就不必关心帧率，只需处理 0 到 1 之间的数值。

渲染器知道每个游戏对象的位置及其当前速度。假设那颗子弹距离屏幕左侧 20 像素，并以每帧 400 像素的速度向右移动。如果当前恰好位于两帧的中间，就会向 `render()` 传入 `0.5`。

因此，渲染器会把子弹绘制在向前半帧的位置，也就是：

```
20 + 400 × 0.5 = 220 像素
```

搞定，运动变得平滑了。

当然，这种外推也可能是错误的。真正计算下一帧时，我们可能会发现子弹撞上了障碍物、速度降低了，或者发生了其他情况。

我们根据子弹上一帧的位置以及自己猜测的下一帧位置，对其渲染位置进行了推算。但在真正完成包含物理和 AI 的完整更新之前，我们并不知道下一帧究竟会发生什么。

所以，这种外推多少带有猜测性质，偶尔会出错。幸运的是，这些修正通常不太明显。至少，它们要比完全不进行外推所造成的卡顿更不容易被察觉。

### 设计决策

尽管本章已经很长，但我省略的内容其实比写进来的更多。一旦加入显示器刷新率同步、多线程和 GPU 等因素，真正的游戏循环可能会变得相当复杂。

不过从高层来看，你很可能需要回答以下几个问题。

### 游戏循环由你控制，还是由平台控制？

这与其说是由你作出的选择，不如说是外部条件替你作出的决定。

如果开发的是运行在网页浏览器中的游戏，基本无法编写传统的游戏循环，因为浏览器基于事件的本质不允许这样做。同样，如果使用现有游戏引擎，你很可能会依赖引擎的游戏循环，而不是自己从头编写。

#### 使用平台的事件循环

实现简单。 你不必担心亲自编写和优化游戏的核心循环。

能够与平台良好配合。 你不必显式给宿主平台留出时间处理自身事件，也不必缓存事件，或者以其他方式处理平台输入模型与你的输入模型之间的不匹配。

失去对时间安排的控制。 平台会在它认为合适的时候调用你的代码。如果调用频率或稳定性达不到要求，你也无能为力。

更糟糕的是，大多数应用程序事件循环都不是为游戏设计的，通常运行缓慢而且不够流畅。

#### 使用游戏引擎的循环

你不必亲自编写。 编写游戏循环可能相当棘手。由于这段核心代码每帧都会执行，哪怕是细小的缺陷或性能问题，也可能给游戏造成巨大影响。

高效而严密的游戏循环，是考虑使用现有引擎的理由之一。

你也无法亲自编写。 当然，硬币的另一面就是失去控制。如果你的需求与引擎提供的循环并不完全吻合，就可能受到限制。

#### 自己编写

拥有完全控制权。 你可以做任何想做的事情，并专门针对游戏的需求设计循环。

必须与平台对接。 应用程序框架和操作系统通常希望获得一部分运行时间，用于处理事件和完成其他工作。如果你完全控制应用程序的核心循环，平台就得不到这些时间。

你必须定期显式交出控制权，以确保框架不会卡死或陷入混乱。

### 如何管理功耗？

五年前，这还不算问题。当时的游戏要么运行在插着电源的设备上，要么运行在专用掌机上。

但随着智能手机、笔记本电脑和移动游戏的兴起，现在你很可能必须考虑功耗。一款游戏即使运行得非常漂亮，如果它先把玩家的手机变成暖手宝，再在三十分钟后耗尽电量，也不会让人高兴。

现在，你可能不仅需要考虑如何让游戏看起来出色，还要尽可能减少 CPU 使用量。通常应该设定一个性能上限：如果一帧需要完成的工作已经全部完成，就让 CPU 进入休眠。

#### 尽可能快地运行

PC 游戏通常会采用这种方式——尽管如今越来越多 PC 游戏也运行在笔记本电脑上。

游戏循环从不显式要求操作系统休眠。所有空闲的处理周期都会用于提高 FPS 或图形质量。

这种方式可以提供尽可能好的游戏体验，但也会消耗尽可能多的电量。如果玩家使用笔记本电脑，就能得到一个不错的腿部取暖器。

#### 限制帧率

移动游戏通常更注重游戏体验，而不是最大限度地提高图形细节。许多移动游戏会为帧率设置上限，通常是 30 或 60 FPS。

如果游戏循环在一帧的时间片耗尽前就完成了处理，它会在剩余时间内休眠。

这种方式先为玩家提供“足够好”的体验，然后不再继续压榨电池。

### 如何控制游戏运行速度？

游戏循环包含两个关键部分：

* 非阻塞式用户输入；
* 适应时间的流逝。

输入相对直接，真正的魔法在于如何处理时间。

游戏可以运行的平台几乎无穷无尽，而同一款游戏可能会在其中许多平台上运行。游戏如何适应这些差异至关重要。

制作游戏似乎是人类的天性。每当我们造出一台能够计算的机器，最先做的事情之一就是在上面制作游戏。

PDP-1 是一台频率只有 2 kHz、内存仅有 4096 个字的机器，但 Steve Russell 和朋友们仍然设法在上面创造了《Spacewar!》。

#### 不进行同步的固定时间步长

这是我们的第一个示例：让游戏循环尽可能快地运行。

实现简单。 这是它主要的——好吧，也是唯一的——优点。

游戏速度直接受到硬件和游戏复杂度影响。 这是它的主要缺点。任何性能变化都会直接影响游戏速度。它就像游戏循环中的固齿自行车：简单直接，却毫无调节能力。

#### 进行同步的固定时间步长

复杂度再提升一级，就是仍然使用固定时间步长，但在循环末尾加入延迟或同步点，避免游戏运行得太快。

仍然相当简单。 与那个可能简单到根本无法实际使用的示例相比，只多了一行代码。

大多数游戏循环本来就可能需要进行某种同步。你很可能会对图形使用双缓冲，并让缓冲区交换与显示器刷新率同步。

对功耗友好。 这对于移动游戏而言重要得令人意外。我们不希望无谓地耗尽用户的电池。

与其试图在每个滴答中塞入越来越多的处理，不如简单地休眠几毫秒，从而节省电量。

游戏不会运行得太快。 它解决了固定循环速度问题的一半。

游戏仍然可能运行得太慢。 如果更新和渲染一帧花费了太长时间，游戏本身就会减速。

由于这种方式没有将更新与渲染分开，它通常比更高级的方案更早遇到这一问题。游戏无法通过丢弃部分渲染帧来追赶时间，只能让游戏进程整体变慢。

#### 可变时间步长

我会把它列为解决方案空间中的一个选项，但要附带警告：我认识的大多数游戏开发者都不建议使用它。不过，记住它为什么是个坏主意仍然很有价值。

它既能适应运行过慢，也能适应运行过快。 如果游戏跟不上现实时间，它会采用越来越大的时间步长，直到追上为止。

它会让游戏变得不确定且不稳定。 这当然是真正的问题所在。特别是物理和网络系统，在可变时间步长下会变得难以处理。

#### 固定更新时间步长，可变渲染频率

示例代码最后介绍的方案最复杂，但也最灵活。它以固定时间步长更新游戏，但在需要追赶玩家时钟时，可以丢弃一些渲染帧。

它既能适应运行过慢，也能适应运行过快。 只要游戏更新能够跟上现实时间，游戏就不会落后。如果玩家的机器性能顶尖，就能提供更加流畅的游戏体验。

实现更加复杂。 它的主要缺点是实现中需要处理更多内容。

你必须调校更新时间步长：既要尽可能小，以便在高端硬件上提供高精度模拟；又不能小到让低端硬件无法及时完成更新。

### 另请参阅

关于游戏循环的经典文章，是 Glenn Fiedler 的《Fix Your Timestep!》。如果没有它，本章不会是现在这个样子。

Witters 关于游戏循环的文章紧随其后，同样非常出色。

Unity 框架拥有一个复杂的游戏循环，这里有一幅精彩的图示详细说明了它。
